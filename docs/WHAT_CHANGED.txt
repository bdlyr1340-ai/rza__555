شنو تغيّر بالضبط؟

الملف الوحيد الذي يتعدل:
bot/handlers/start.py

التغييرات:
- إضافة import os إذا غير موجود.
- استبدال دالة _build_pixel_progress فقط حتى تعرض:
  ⬜ / 🔄 / 🟩 / 🟥
- تغيير حساب الوقت من time.time إلى time.monotonic.
- تغيير ticker من 3 ثواني إلى 1 ثانية.
- عند النجاح، إذا ماكو screenshot في result، يستخدم اللقطة الموجودة أصلاً:
  /tmp/gemini_claim_page.png
- عند الفشل، يستخدم لقطة debug الموجودة أصلاً.
- منع إرسال summary و html_path حتى تبقى لقطة شاشة فقط.

ماكو تعديل على:
- bot/services/sheerid.py
- قاعدة البيانات
- الأزرار
- ملفات الخدمات
- منطق البوت القديم
